"""terminal_vs.sim.state - mutable simulation state buffers (stub until Day 3).

Will hold SimState (player, enemies, projectiles, pickups) and new_run(cfg, rng).
The mutable entity buffers are the "mutable" side of the section 6 boundary and
must not leak outside the sim layer. Pure stub; no blessed import.
"""

from __future__ import annotations
