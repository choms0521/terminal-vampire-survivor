"""terminal_vs.render.frame - layered frame rendering (stub until Day 5).

Will hold render_frame(term, state, cam, cfg): cull to the visible region and
composite by draw priority (floor < pickup < enemy < projectile < player < HUD,
player always on top). blessed is imported here only in Day 5. Pure stub for now.
"""

from __future__ import annotations
