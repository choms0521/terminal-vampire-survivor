"""terminal_vs.rules.weapons - weapon firing logic (stub until Phase 1 Day 4).

Will hold select_target / should_fire / spawn_projectile_intent as pure
functions. Nearest-enemy targeting ties break on lowest entity id (deterministic,
unit-testable). It will import sq_dist_aspect from terminal_vs.world for the
single aspect-distance definition. Pure stub; no blessed import.
"""

from __future__ import annotations
